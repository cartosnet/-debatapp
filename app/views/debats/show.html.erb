
<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://unsplash.com/photos/jrh5lAq-mIs');">
  <div class="banner-content">
    <h1><%= @debat.description %></h1>
    <p>Il se termine le </p>
    <h2><%= @debat.due_date %></h2>
  </div>
</div>

<div class="container">
  <div class="row">
<h2>Votes Results</h2>

	<% @votes.each do |vote| %>
	
		<div class="vote">

		
			<img class="<%= vote.value ? "avatar-blue" : "avatar-red" %> img-circle" src="https://kitt.lewagon.com/placeholder/users/gabriel-dehan"> 		
			<div class="vote-name">
				<h3><%= vote.user.firstname %></h3>
				<p><%= vote.created_at.strftime("Publié le %m/%d/%Y")%></p>
			</div>
			<div class="vote-body">
				<p><%= vote.description %></p>
			</div>
		</div>
	<% end %>
