<%= simple_form_for([ debat, vote ]) do |form| %>
  <%= form.input :value, as: :radio_buttons, collection: [ "pro", "contre"] %>
  <%= form.input :description, as: :text %>
  <%= form.button :submit %>
<% end %>