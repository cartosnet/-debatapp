<%= simple_form_for([ debat, vote ]) do |form| %>
  <%= form.input :value, as: :radio_buttons, collection: [ "Oui complétement", "Non je pense pas"] %>
  <%= form.input :description, as: :text %>
  <%= form.button :submit %>
<% end %>